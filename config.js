/* ═══════════════════════════════════════════════════════════════════
   The only file you edit by hand.

   SCRIPT_URL — the Apps Script Web app URL from SETUP.md step 3.
                It ends in /exec. Leave blank and both pages fall back
                to the built-in sample data.

   Safe to commit: this URL is read-only to the public. The write token
   is never stored here — the console keeps it in your browser only.
   ═══════════════════════════════════════════════════════════════════ */

window.VIVELY_CONFIG = {
  SCRIPT_URL: "",
  ORG: "VIVELY GLOBAL"
};
